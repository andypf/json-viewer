import ExpandContainer from "../../src/renderer/expand-container"

describe("ExpandContainer", () => {
  it("should create an expandable container", () => {
    // const container = new ExpandContainer({})
    // expect(container.element.className).toEqual("expand-container")
  })
})
