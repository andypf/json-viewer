import "../src/component.js"

describe("JsonViewer", () => {
  it("should be defined", () => {
    // document.body.innerHTML = `<h1>Custom element test</h1> <json-viewer></json-viewer>`
    // expect(document.body.innerHTML).toContain("It works!")
  })
})
